import router from "@/router/router"
// import {isLogin} from "@/utils/auth"

// router.beforeEach((to, from, next) => {
//     // * 判断是否有Token
//     const globalStore = isLogin();
//     if (globalStore) {
//         console.log("no longin");
//         return;
//     }
// })
export default router;